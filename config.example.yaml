# Modalità CLI legacy — per uso normale apri orgchart_app.py (GUI).
# La GUI usa orgchart_defaults.yaml + local_session.yaml + CSRF incollato.

session_token: "YOUR_SESSION_TOKEN_HERE"
csrf_token: "YOUR_CSRF_TOKEN_HERE"
